import Loading from '@/components/loading';
import React from 'react';

function MovieLoading(props) {
    return (
        <div style={{height: "100%"}}>
            
            <Loading/>
        </div>
    );
}

export default MovieLoading;